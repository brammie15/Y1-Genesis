<Window x:Class="Y1_ingester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Y1_ingester"
        xmlns:viewModels="clr-namespace:Y1_ingester.ViewModels"
        mc:Ignorable="d"
        Title="Y1-Genesis - Brammie15" Height="600" Width="950">

    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <!-- Top Toolbar -->
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Settings" />
            <Button Content="Refresh" Command="{Binding RefreshSongsCommand}" />
            <Button Content="Rescan Devices" Command="{Binding RefreshDevicesCommand}" />
            <Button Content="Hard Sync" Command="{Binding HardSyncCommand}"/>
            <Button Content="Help / Issues" Command="{Binding OpenIssuesCommand}"/>
        </ToolBar>

        <!-- Main Content Area -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="10" DockPanel.Dock="Top" Grid.Row="1">
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="150" ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" />
            </StackPanel>

            <!-- Left Side: Song List -->
            <ScrollViewer Grid.Column="0">
                <ItemsControl ItemsSource="{Binding SongsView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="3" Padding="0" BorderThickness="2" CornerRadius="5" BorderBrush="{Binding BorderColor}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Image Source="{Binding AlbumArt}" Width="120" Height="70" Margin="5"/>

                                    <StackPanel Margin="5" Width="300">
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" TextWrapping="WrapWithOverflow"/>
                                        <TextBlock Text="{Binding Artist}" FontStyle="Italic" TextWrapping="WrapWithOverflow"/>
                                        <TextBlock Text="{Binding Album}" TextWrapping="WrapWithOverflow"/>
                                        <TextBlock Text="{Binding Year}"/>
                                    </StackPanel>

                                    <Button Grid.Column="2"
                                    Command="{Binding DataContext.DeleteSongCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Width="32" Height="32"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    ToolTip="Delete Song"
                                    Background="Transparent"
                                    BorderBrush="{x:Null}"
                                    Padding="0">    
                                        <TextBlock Text="🗑" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Button>

                                    <Button Grid.Column="2"
                                    Command="{Binding DataContext.EditSongCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Width="32" Height="32"
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    ToolTip="Edit Song"
                                    Background="Transparent"
                                    BorderBrush="{x:Null}"
                                    Padding="0">                    
                                        <TextBlock Text="✏" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top">
                <TextBlock Text="Upload Options" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                <TextBlock Text="Select Drive:"/>
                <ComboBox Name="DriveSelector" Margin="0,0,0,0" ItemsSource="{Binding RockboxDrives}" SelectedValue="{Binding SelectedRockboxDrive}">
                </ComboBox>

                <TextBlock Text="Upload Songs:" Margin="0,10,0,10"/>
                <Button Content="Upload To Device" Width="150" Margin="0,0,0,20" IsEnabled="{Binding IsRockBoxConnected}" Command="{Binding UploadSongsCommand}"/>

                <TextBlock Text="Add Song from URL:"/>
                <TextBox Name="UrlInput" Width="200" Margin="0,10,0,10" Text="{Binding UrlText, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Button Content="Download and Add" Width="150" Margin="0,0,0,20" Command="{Binding DownloadUrlCommand}" CommandParameter="{Binding ElementName=UrlInput, Path=Text}"/>

                <ListBox ItemsSource="{Binding QueuedSongs}" Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Title}" Width="200"/>
                                <TextBlock Text="{Binding Status}" Margin="10,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
